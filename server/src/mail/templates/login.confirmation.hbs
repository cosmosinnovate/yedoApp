<!DOCTYPE html>
<html>

<head>
  <meta charset="UTF-8">
  <title>Email verification</title>
  <link href="https://fonts.googleapis.com/css?family=Roboto:300,400,500,700&display=swap" rel="stylesheet">
  <style>
    .content {
      width: 100%;
      max-width: 500px;
      margin: 0 auto;
      padding: 20px;
      background: #fff;
      border: 1px solid #eee;
      border-radius: 5px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      font-family: sans-serif;
      font-size: 16px;
      line-height: 1.5;
      color: #555;
    }

    .content .header {
      text-align: center;
      padding: 20px 0;
    }

    .content.title {
      text-align: center;
      font-size: 1.2em;
      margin-bottom: 0;
      padding: 10px 0;
    }

    .content img {
      text-align: center;
      font-size: 1.2em;
      margin-bottom: 0;
      padding: 10px 0;
    }

    .content .divider {
      width: 100%;
      height: 1px;
      background: #eee;
      margin: 10px auto 20px;
    }

    h1 {
      font-size: 16px;
      font-weight: bold;
      position: relative;
      margin-top: 0;
      color: #2d2d2d;
      font-size: 1.5em;
      display: inline-block;
    }

    p {
      margin-top: 0;
      margin-bottom: 1rem;
      font-size: 1em;
    }
  </style>
</head>

<body>
  <div class='content'>
    <div class='header'>
      {{!-- <img src="https://yedo-assets.nyc3.cdn.digitaloceanspaces.com/yedo-logo.png" width="120" /> --}}
      <h3 class='title'>{{title}}</h3>
    </div>
    <div class='divider'></div>
    <p>{{topBody}}</p>
    {{#if otp}}
      <h1> {{otp}} </h1>
    {{/if}}

    <p>Thanks,<br>Team yedo</p>
  </div>
</body>

</html>